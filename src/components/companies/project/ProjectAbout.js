import React from "react";
import PropTypes from "prop-types";

const ProjectAbout = ({ projectInfo }) => {
  return <div></div>;
};

ProjectAbout.propTypes = {
  projectInfo: PropTypes.object.isRequired
};

export default ProjectAbout;
